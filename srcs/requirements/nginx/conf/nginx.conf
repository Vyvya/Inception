# events {
#     worker_connections 1024;  # Adjust the number as needed
# }

# http {
	server {
		#SSL/TLS Configuration
		listen 443 ssl;
		listen [::]:443 ssl;

		ssl_protocols TLSv1.2 TLSv1.3;

		ssl_certificate /etc/nginx/ssl/vgejno.crt;
		ssl_certificate_key /etc/nginx/ssl/vgejno.key;

		#root and index and server_name
		root /var/www/html;
		server_name vgejno.42.fr;
		index index.php index.html index.htm index.nginx-debian.html test.php;

		#location wordpress
		location / {
			try_files $uri $uri/ /index.php$is_args$args;
			autoindex on;
			autoindex_exact_size on;
		}

		#php request
		location ~ \.php$ {
			fastcgi_split_path_info ^(.+\.php)(/.+)$;
			fastcgi_pass wordpress:9000;
			fastcgi_index index.php;
			include fastcgi_params;
			fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
			fastcgi_param SCRIPT_NAME $fastcgi_script_name;
		}

		location = /favicon.ico {
		log_not_found off;
		access_log off;
		}
	}
# }